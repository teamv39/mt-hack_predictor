# 🤖 Инструкция для AI-агентов (AGENTS.md)

> **Проект:** Интеллектуальный ситуационный предиктор сбоев и интервалов движения  
> **Контекст:** MT-Hackathon (Хакатон Московского Транспорта), Трек №3  
> **Тимлид:** Денис (@shteppinson)

---

## 1. Концепция и продуктовая суть

Мы разрабатываем не просто прогноз времени прибытия (ETA), а **проактивную Систему Поддержки Принятия Решений (СППР / DSS)** для диспетчеров городского транспорта (Дептранс Москвы / Мосгортранс / ЦОДД).

* **Главная боль города:** «Пачкование» автобусов (*Bus Bunching*) и схлопывание интервалов (*Headway collapse*) — когда пассажиры ждут 25 минут, а затем приходят 3 автобуса подряд.
* **Наше решение:**
  1. **Предикт за 15–40 минут:** ML заранее выявляет зоны риска деградации интервалов.
  2. **Объяснимый ИИ (XAI):** Факторный анализ на базе SHAP показывает причину (затор на перегоне, задержка посадки из-за дождя, светофор).
  3. **Actionable UI (Готовое действие):** Кнопка прямого действия для диспетчера: *«Придержать борт №1043 на 2.5 мин на остановке Х»* (стратегия *Holding*), предотвращающая сбой до его наступления.

---

## 2. Команда и роли

* **Денис (Team Lead, Product, Go Backend):**
  * Архитектура системы, согласование API-контрактов, защита проекта и питч.
  * Высокопроизводительный бэкенд на **Go** (микросекундный latency для телеметрии, Data Feeder, REST/WebSocket API).
* **Артём (Data Engineer):**
  * Очистка «грязного» GPS/ГЛОНАСС, Map-matching на дорожный граф и остановки.
  * Расчет динамического **Headway** (дельта к переднему/заднему бортам), кумулятивных задержек, скользящих скоростей.
* **Миша (Lead ML / Data Scientist):**
  * Модели CatBoost / LightGBM: регрессия времени хода $\Delta t$, классификация риска пачкования.
  * Валидация через `TimeSeriesSplit` (защита от Data Leakage).
  * Расчет весов SHAP и микросервис инференса (FastAPI / ONNX).
* **Кирилл (Frontend Developer / UI/UX):**
  * Ситуационный дашборд диспетчера (React, Mapbox/Leaflet, Slate Dark Mode `#0B0F17`).
  * Живая карта с маркерами и связками бортов, предиктивный радар алертов, сравнительный график *«План vs Без мер vs С рекомендацией ИИ»*.

---

## 3. Структура репозитория

```
mt-hack_predictor/
├── AGENTS.md                  # Данная инструкция для AI
├── CLAUDE.md                  # Указатель на AGENTS.md
├── README.md                  # Полный паспорт проекта и дорожная карта хакатона
├── .gitignore                 # Универсальный gitignore (Go, Node, Python, Data)
├── .gitattributes             # Нормализация LF и бинарных файлов
├── backend/                   # Высокопроизводительный Go-бэкенд (Денис)
│   ├── cmd/server/main.go     # Точка входа HTTP/WS сервера (Chi)
│   ├── internal/
│   │   ├── api/               # Маршруты и контроллеры
│   │   ├── engine/            # Алгоритмы рекомендаций (Holding, Headway)
│   │   ├── feeder/            # Эмулятор потока телеметрии из логов
│   │   └── models/            # Доменные структуры (Vehicle, Alert, Recommendation)
│   ├── go.mod
│   └── go.sum
├── frontend/                  # React + Vite дашборд диспетчера (Кирилл)
│   ├── src/                   # Компоненты карты, радара, инспектора
│   ├── package.json
│   └── vite.config.js
├── ml/                        # ML пайплайны и инференс (Миша & Артём)
│   ├── pyproject.toml         # Зависимости uv (CatBoost, LightGBM, SHAP, FastAPI)
│   ├── notebooks/             # EDA, эксперименты, валидация
│   └── src/
│       ├── api/server.py      # FastAPI инференс-сервис
│       ├── features/          # Расчет Headway, Map-matching
│       └── models/            # Обучение и экспорт моделей
├── data/                      # Данные (исключены из Git, кроме .gitkeep)
│   ├── raw/                   # Исходные треки, GTFS, логи
│   ├── processed/             # Обработанные датасеты с фичами
│   ├── sample/                # Небольшие тестовые срезы данных
│   └── models/                # Экспортированные веса моделей (.cbm, .onnx)
└── docs/                      # Архитектура, бизнес-логика и API контракты
    ├── team_handbook.md       # Настольная книга команды: теория, библиотеки, стратегия победы
    ├── architecture.md        # Спецификация компонентов и потоков данных
    ├── business_values.md     # Описание болей, метрик и экономического эффекта
    └── api_contracts.md       # JSON-схемы REST и WebSocket
```

---

## 4. Команды для работы и проверки сборки

### Go Backend:
```bash
cd backend
go run ./cmd/server         # Запуск сервера на :8080
go test ./...               # Запуск тестов
go build -o bin/server ./cmd/server # Проверка компиляции
```

### Frontend (React + Vite):
```bash
cd frontend
npm install                 # Установка зависимостей
npm run dev                 # Запуск dev-сервера на :5173
npm run build               # Проверка production-сборки
```

### ML & Data (uv / Python):
```bash
cd ml
uv sync                     # Установка зависимостей в venv
uv run uvicorn src.api.server:app --reload --port 8000 # Запуск сервиса предиктов
```

---

## 5. Правила и договоренности для кода

1. **Контракты превыше всего:** Любые изменения форматов телеметрии или алертов согласуются и сразу вносятся в [backend/internal/models/types.go](file:///Users/shteppinson/dev/hacks/mt-hack_predictor/backend/internal/models/types.go) и [docs/api_contracts.md](file:///Users/shteppinson/dev/hacks/mt-hack_predictor/docs/api_contracts.md).
2. **Продакшн-мышление:** Избегать "игрушечного" кода. Бэкенд должен выдерживать высокую частоту обновлений телеметрии.
3. **Объяснимость обязательна:** Каждая рекомендация модели должна сопровождаться SHAP-факторами (почему система приняла такое решение).
4. **Валидация во времени:** В ML строго использовать `TimeSeriesSplit`, заглядывание в будущее недопустимо.
5. **UI-стандарты:** Тёмный строгий дизайн Ситуационного центра ЦОДД (`#0B0F17` / `#0F172A`), парящие полупрозрачные панели, микроанимации и четкие цветовые маркеры (зеленый/желтый/красный).
